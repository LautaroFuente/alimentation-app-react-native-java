import { View, Button } from "react-native";
import LoginForm from "./components/loginForm";

const Login = () => {
  return (
    <View>
      <Button title="Volver" />
    </View>
  );
};

export default Login;
